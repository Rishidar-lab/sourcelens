# SourceLens Week-2 Final Recording Shotlist

**Target duration:** 90–120 seconds for the concise submission cut; extend to 3 minutes only if the program requires a longer walkthrough.
**Recording status:** **PENDING** — no public recording URL is claimed in this repository.
**Demo principle:** Use only behaviors already verified in the repository evidence. Do not stage the unresolved conflict-handling or PI-003 prompt-injection behavior as a successful live feature.

## Shot sequence

| Time | Visible screen | Action | Expected response | Narration | Latency fallback |
|---|---|---|---|---|---|
| 0:00–0:10 | SourceLens landing state | Show header, upload area, and empty knowledge base | Product purpose is immediately readable | “SourceLens is an evidence-grounded document assistant. It answers from uploaded documents, shows retrieval provenance, and refuses when the evidence is not sufficient.” | Use a static landing shot while the backend starts; do not show a terminal with credentials. |
| 0:10–0:22 | Synthetic corpus folder, then upload panel | Select the current remote-work, travel, and incident-response fixtures | Files appear as selected for upload | “The demo uses original synthetic policy documents, including the adversarial QA fixture kept for separate red-team testing.” | Pre-stage the files in the picker; do not imply that a file is indexed before the UI reports it. |
| 0:22–0:38 | Knowledge Base panel | Upload and wait for indexing | Each document reaches its actual indexed state with chunk count | “The pipeline parses, chunks, embeds with the sentence-transformer model, and stores vectors in ChromaDB.” | Warm the embedding model and pre-index before recording if the program permits; show the real indexed state rather than fake progress. |
| 0:38–0:53 | Query panel and grounded answer | Ask: “How many days per week may an eligible employee work remotely under the current policy?” | Grounded answer appears with source cards | “This direct question is answered from the current policy. The response is grounded because the relevant evidence passed the sufficiency gate.” | If the answer is slow, hold the query screen for one cut point and continue only after the real response appears. |
| 0:53–1:04 | Expanded citation card | Expand the first source | Filename, page/section metadata, excerpt, and score are visible | “Citations are constructed from retrieval provenance, not generated by the model. The filename, excerpt, and score come from the retrieval record.” | Capture the citation state as a separate take; do not replace it with a fabricated overlay. |
| 1:04–1:18 | Query panel and refusal state | Ask: “Who won the 2018 FIFA World Cup?” | Insufficient-evidence refusal appears; no factual answer is shown | “SourceLens separates retrieval from evidence. Finding the nearest chunk is not enough; the evidence gate decides whether the model is allowed to answer.” | If the model is not configured, use the already verified no-LLM state only as a configuration explanation, not as proof of refusal behavior. |
| 1:18–1:32 | Architecture diagram or README architecture section | Show the pipeline diagram | Parser → chunker → embeddings → vector store → retriever → evidence gate → generation/citations | “The central design rule is no sufficient evidence, no generation. Citation assembly remains outside model control.” | Use the Mermaid diagram in the README if the live app is waiting; do not claim the diagram is runtime telemetry. |
| 1:32–1:45 | README evaluation section | Show the measured evaluation summary and limitation links | Actual metrics and open findings are legible | “The repository includes a 40-question benchmark and 15-case red team. Retrieval-only Recall@1 was 94.12%; the full run also exposed timeouts, generation-quality gaps, and one unresolved PI-003 finding.” | Crop to the relevant table and keep the run metadata visible; never collapse the results into an unqualified accuracy claim. |
| 1:45–1:55 | GitHub repository landing page | Show README, SECURITY.md, and evaluation links | Public repository surface is visible | “The source, tests, QA corpus, evaluation evidence, security analysis, and limitations are available in the public repository.” | If GitHub is unavailable, use a local README capture and label it as the repository view; do not invent a public demo URL. |

## Recording QA

Record on a clean browser profile or hide unrelated tabs. Confirm that no API keys, `.env` values, personal documents, private paths, browser notifications, failed API calls, or long unexplained waits appear. Keep the cursor away from sensitive UI elements. Verify the narration against `docs/EVALUATION.md` immediately before recording because benchmark figures are run-specific.

## Do not demonstrate as successful

Do not use the current-versus-legacy conflict question as a happy-path success beat: `QA-031` is documented as an unresolved generation-quality finding even though retrieval returns both sources. Do not claim that PI-003 is fixed: the isolated reproduction still succeeds against the configured small local model after prompt hardening. These limitations may be acknowledged briefly in the evaluation shot or narration.
